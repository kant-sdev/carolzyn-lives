# Local environment file (safe defaults)
# Secrets should be stored in `.env.local` (gitignored).
# Copy `.env.example` → `.env.local` and fill the secret values.

TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
TWITCH_ACCESS_TOKEN=
TWITCH_USERNAME=carolzyn
BIBLIA_API_TOKEN=
